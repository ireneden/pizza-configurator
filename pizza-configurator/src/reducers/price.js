import { NEW_CHOICE } from '../actions/choice'


export default (state = initialState, { type, payload } = {}) => {
    switch(type) {
    //   case NEW_CHOICE:
    //   if (){
    //     return }
    //     else {return }
    //
    // default:
    //   return state

    }}


    // export default (state = initialState, action) => {
    //   switch (action.type) {
    //     case NEW_CHOICE :
    //         price = price.concat(action.payload.price)
    //         const newState = newChoice(chosenOption, price)
    //         return newState
    //
    //     default :
    //       return state
    //
    // }
    // }
